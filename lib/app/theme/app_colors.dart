import 'package:flutter/material.dart';

abstract class AppColors {
  // Primary — deep Islamic green/teal
  static const primary = Color(0xFF1a5276);
  static const primaryLight = Color(0xFF2980b9);
  static const primaryDark = Color(0xFF0d2b3e);

  // Accent — gold
  static const accent = Color(0xFFD4AC0D);
  static const accentLight = Color(0xFFF9E79F);

  // Backgrounds
  static const backgroundLight = Color(0xFFF8F9FA);
  static const backgroundDark = Color(0xFF0d1117);
  static const surfaceLight = Color(0xFFFFFFFF);
  static const surfaceDark = Color(0xFF161B22);
  static const cardDark = Color(0xFF1C2128);

  // Text
  static const textPrimary = Color(0xFF1A1A2E);
  static const textSecondary = Color(0xFF6C757D);
  static const textLight = Color(0xFFFFFFFF);

  // Semantic
  static const success = Color(0xFF27AE60);
  static const warning = Color(0xFFE67E22);
  static const error = Color(0xFFE74C3C);

  // Prayer time colors
  static const fajr = Color(0xFF1A3A5C);
  static const dhuhr = Color(0xFFD4AC0D);
  static const asr = Color(0xFFE67E22);
  static const maghrib = Color(0xFFE74C3C);
  static const isha = Color(0xFF2C3E50);
}
